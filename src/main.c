#include <app.h>
#include <stdio.h>
int main(void)
{
/*   App app;
  AppCreateInfo create_info = {"Dorton"};

  app_create(&app, &create_info);
  app_run(&app);
  app_destroy(app);
  
  getchar(); */
  test(2);

  return 0;
}